<Page
    x:Class="TTS_Live_UI.Views.EditorDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea" Padding="16">
        <StackPanel Spacing="16">
            <TextBlock
                Text="Detalhes do Atalho"
                Style="{ThemeResource TitleTextBlockStyle}"/>

            <StackPanel Spacing="8">
                <TextBlock Text="Rótulo" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                <TextBlock
                    Text="{x:Bind ViewModel.ShortcutLabel, Mode=OneWay}"
                    Style="{ThemeResource BodyTextBlockStyle}"/>
            </StackPanel>

            <StackPanel Spacing="8">
                <TextBlock Text="Texto" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                <TextBlock
                    Text="{x:Bind ViewModel.ShortcutText, Mode=OneWay}"
                    Style="{ThemeResource BodyTextBlockStyle}"
                    TextWrapping="Wrap"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
